# =============================================================================
# Streamlit Cloud deployment requirements
#
# This is a SLIM list. Only what the Streamlit app actually needs at runtime.
# It deliberately excludes training-only deps (lightgbm, statsmodels, tqdm)
# because the demo only LOADS the saved artifacts -- it never re-trains.
#
# torch is pulled from the CPU-only wheel index (much smaller install, no
# GPU on Streamlit Cloud anyway).
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cpu

# Core scientific stack
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# Deep learning (CPU build)
torch>=2.1

# Data acquisition
yfinance>=0.2.40

# Plotting + UI
plotly>=5.20
streamlit>=1.32

# Utilities
pyyaml>=6.0
